import React from "react";
// import

const Button = ({ name }) => {
  return (
    <div className="button">
      <button class="btn-hover color">{name}</button>
    </div>
  );
};

export default Button;
